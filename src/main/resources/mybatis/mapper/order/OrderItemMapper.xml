<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ecommerce.platform.domain.order.mapper.OrderItemMapper">

    <!-- Result Map -->
    <resultMap id="OrderItemResultMap" type="com.ecommerce.platform.domain.order.entity.OrderItem">
        <id property="id" column="id"/>
        <result property="price" column="price"/>
        <result property="quantity" column="quantity"/>
        <result property="subtotal" column="subtotal"/>
        <association property="product" javaType="com.ecommerce.platform.domain.product.entity.Product">
            <id property="id" column="product_id"/>
            <result property="name" column="product_name"/>
            <result property="price" column="product_price"/>
        </association>
    </resultMap>

    <!-- 주문 항목 저장 -->
    <insert id="insert" parameterType="com.ecommerce.platform.domain.order.entity.OrderItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (order_id, product_id, price, quantity, subtotal)
        VALUES (#{order.id}, #{product.id}, #{price}, #{quantity}, #{subtotal})
    </insert>

    <!-- 주문 ID로 주문 항목 조회 -->
    <select id="findByOrderId" resultMap="OrderItemResultMap">
        SELECT oi.id, oi.price, oi.quantity, oi.subtotal,
               oi.product_id, p.name as product_name, p.price as product_price
        FROM order_items oi
        INNER JOIN products p ON oi.product_id = p.id
        WHERE oi.order_id = #{orderId}
    </select>

    <!-- ID로 주문 항목 조회 -->
    <select id="findById" resultMap="OrderItemResultMap">
        SELECT oi.id, oi.price, oi.quantity, oi.subtotal,
               oi.product_id, p.name as product_name, p.price as product_price
        FROM order_items oi
        INNER JOIN products p ON oi.product_id = p.id
        WHERE oi.id = #{id}
    </select>

    <!-- 주문 항목 삭제 -->
    <delete id="deleteById">
        DELETE FROM order_items
        WHERE id = #{id}
    </delete>

    <!-- 주문 ID로 전체 주문 항목 삭제 -->
    <delete id="deleteByOrderId">
        DELETE FROM order_items
        WHERE order_id = #{orderId}
    </delete>

</mapper>