<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ecommerce.platform.domain.user.mapper.UserMapper">

    <!-- Result Map -->
    <resultMap id="UserResultMap" type="com.ecommerce.platform.domain.user.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 회원 저장 -->
    <insert id="insert" parameterType="com.ecommerce.platform.domain.user.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, name, role, created_at, updated_at)
        VALUES (#{email}, #{password}, #{name}, #{role}, NOW(), NOW())
    </insert>

    <!-- ID로 회원 조회 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT id, email, password, name, role, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 이메일로 회원 조회 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, email, password, name, role, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 전체 회원 조회 -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT id, email, password, name, role, created_at, updated_at
        FROM users
        ORDER BY id DESC
    </select>

    <!-- 회원 수정 -->
    <update id="update" parameterType="com.ecommerce.platform.domain.user.entity.User">
        UPDATE users
        SET email = #{email},
            password = #{password},
            name = #{name},
            role = #{role},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 회원 삭제 -->
    <delete id="deleteById">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

</mapper>